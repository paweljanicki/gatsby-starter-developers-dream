import { AnchorLink } from 'gatsby-plugin-anchor-links';
import { BlogPostsList, Video, CodePen, ExternalLink } from './.';

export const shortcodes = {
  BlogPostsList,
  Video,
  CodePen,
  ExternalLink,
  AnchorLink,
};
